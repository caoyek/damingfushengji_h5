# 📊 《大明浮生记》项目现有资源 VS 全量解包资源深度对比报告

本文档记录了 `damingfushengji_h5` 仓库原有资源与本次从真机解包提取的 **全量完整工程资产 (`extracted_full_resources/`)** 之间的详细对比与差异分析。

---

## 📈 一、 资源总览与体量对比

| 资源维度 | 仓库原有资源 (`damingfushengji_h5`) | 本次新存入全量资源 (`extracted_full_resources/`) | 差异与补全情况 |
| :--- | :--- | :--- | :--- |
| **美术图片 (PNG)** | **659 张** (约 10 MB) | **10,213 张** (约 35 MB) | 🟢 **新增 9,554 张** (涵盖全量怪物、全装备、全特效、全地图) |
| **音频音效 (OGG)** | **0 首** (缺失) | **53 首** (约 9.5 MB) | 🟢 **完整补全** (16 首 BGM 音乐 + 37 种门派技能音效) |
| **UI 界面布局 (XML)** | 部分转录在 `pages-data.js` 中 | **347 个标准原始 XML** | 🟢 **完整补全** (原汁原味像素级坐标与层级属性) |
| **动画映射序列 (ANI)** | **0 个** (缺失) | **13 个标准 ANI 文件** | 🟢 **完整补全** (包含 15,953 帧动作映射的 `army.ani` 等) |
| **核心数据库表 (FDB)** | 少量硬编码在 `runtime-seed.js` | **31 张数据表 (提供 JSON/CSV 双格式)** | 🟢 **完整补全** (全城池、野外据点、技能时序、阵位、剧情台词) |
| **设计与协议文档 (Docs)** | 仅 README 说明 | **5 篇 GDD 策划案 + TDD 协议案 + 数据字典** | 🟢 **完整补全** (职业、数值公式、装备成长、搞丸、通信协议) |

---

## 🔍 二、 之前场景容易出现“关联性错误/缺失”的根本原因分析

对比您的 H5 项目代码与原始全量资产，之前在开发后续场景时频繁报错的主要原因如下：

### 1. 🖼️ 图片素材存在大面积断层 (之前仅有 6.4% 的图片)
* **之前的情况**：仓库原有 659 张图只覆盖了登录、创角、新手村、小部分初级怪物和基础 UI。
* **报错原因**：一旦玩家离开新手村，访问成都/宁波/武汉等高级主城、挑战 B50 之后的 570+ 种副本怪物、穿戴高级装备、释放门派进阶技能、或者进入倒卖场/大牢打工时，前端由于**找不到对应的贴图直接报 404**，导致场景白块或崩溃。

### 2. 🔗 缺少关键的【ANI 别名二次映射】
* **之前的情况**：代码直接尝试根据逻辑名称去匹配图片。
* **报错原因**：原版底层所有的贴图不是直连的，而是通过 `ui.ani` / `weapon.ani` / `fightroad.ani` 建立的“逻辑名 $ightarrow$ 真实 PNG”映射（例如 `[image/monster/B1.gif] -> data/monster/b1.png`）。缺少了 13 个 `.ani` 文件，就无法准确索引到真实的切图帧。

### 3. 🧩 UI 嵌套控件缺少原生 XML 支撑
* **之前的情况**：手动将部分 XML 提炼转录为 JS 数据，维护成本高且容易遗漏深层模板。
* **本次改进**：将全量 347 个原版 XML 完整归档，支持直接通过统一的 XML 解析器自动递归加载子模板（如 `PackItem`, `ItmTalent`）。

---

## 📁 三、 新存入的资源目录索引 (`extracted_full_resources/`)

新建立的资源文件夹位于：`damingfushengji_h5/extracted_full_resources/`

```text
extracted_full_resources/
├── docs/                       # 【设计与技术规范文档】
│   ├── GDD/                    # 5 篇策划设计案（职业、战斗数值公式、装备强化、搞丸系统等）
│   ├── TDD/                    # 技术架构案（通信协议、CMsg 接口表）
│   └── DataDict/               # 数据字典（31 张数据表索引说明）
│
├── client_assets/              # 【客户端全量资产】
│   ├── images/                 # 10,213 张高清透明 PNG 贴图
│   ├── audio/                  # 53 首音频（bgm/ 背景音乐、sfx/ 音效、skill/ 技能音效）
│   ├── ui_layouts/             # 347 个标准 XML 界面布局文件
│   ├── animations/             # 13 个 ANI 动作序列文件
│   └── fonts/                  # 游戏内置矢量中文字体 (font.ttf)
│
├── client_configs/             # 客户端原始配置 (platformcfg.xml 等)
│
├── server_data/                # 【全量 31 张核心数据表】
│   ├── tables_json/            # JSON 格式数据表（程序直接 require / fetch）
│   └── tables_csv/             # CSV / Excel 格式数据表（直观查看与编辑）
│
└── tools/                      # 【工具链与解析脚本】
    └── scripts/                # WDF 提取器、KFDB 解析器、资源分类工具
```
